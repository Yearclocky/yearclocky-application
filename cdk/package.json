{
  "name": "yearclocky-cdk",
  "version": "1.0.0",
  "private": true,
  "description": "AWS CDK configuration for the Yearclocky application.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cdk": "cdk",
    "clean": "rm -rf cdk.out dist",
    "synth": "cdk synth",
    "watch": "tsc -w -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.190.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.190.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
